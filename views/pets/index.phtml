<?php require_once 'views/templates/header.phtml'; ?>

<div class="d-flex justify-content-between align-items-center mb-4">
    <h2>Missing & Found Pets</h2>
    <?php if(isset($_SESSION['user_id'])): ?>
        <a href="index.php?controller=pet&action=create" class="btn btn-success">Report Missing Pet</a>
    <?php endif; ?>
</div>

<div class="card shadow-sm mb-4 bg-light border-0">
    <div class="card-body">
        <form action="index.php" method="GET" class="row g-3">
            <input type="hidden" name="controller" value="pet">
            <input type="hidden" name="action" value="index">
            
            <div class="col-md-4">
                <input type="text" name="keyword" class="form-control" placeholder="Search names or descriptions..." value="<?= htmlspecialchars($keyword ?? '') ?>">
            </div>
            <div class="col-md-3">
                <select name="type" class="form-select">
                    <option value="">All Types</option>
                    <option value="Dog" <?= (isset($type) && $type === 'Dog') ? 'selected' : '' ?>>Dog</option>
                    <option value="Cat" <?= (isset($type) && $type === 'Cat') ? 'selected' : '' ?>>Cat</option>
                    <option value="Bird" <?= (isset($type) && $type === 'Bird') ? 'selected' : '' ?>>Bird</option>
                    <option value="Other" <?= (isset($type) && $type === 'Other') ? 'selected' : '' ?>>Other</option>
                </select>
            </div>
            <div class="col-md-3">
                <select name="status" class="form-select">
                    <option value="">All Statuses</option>
                    <option value="missing" <?= (isset($status) && $status === 'missing') ? 'selected' : '' ?>>Missing</option>
                    <option value="found" <?= (isset($status) && $status === 'found') ? 'selected' : '' ?>>Found</option>
                </select>
            </div>
            <div class="col-md-2 d-grid">
                <button type="submit" class="btn btn-primary">Search</button>
            </div>
        </form>
    </div>
</div>

<div class="row">
    <?php if (empty($pets)): ?>
        <div class="col-12 text-center py-5">
            <h4 class="text-muted">No pets found matching your criteria.</h4>
            <a href="index.php?controller=pet&action=index" class="btn btn-outline-secondary mt-2">Clear Filters</a>
        </div>
    <?php else: ?>
        <?php foreach ($pets as $pet): ?>
            <div class="col-md-4 mb-4">
                <div class="card h-100 shadow-sm border-<?= $pet['status'] === 'missing' ? 'danger' : 'success' ?>">
                    <div class="card-header bg-<?= $pet['status'] === 'missing' ? 'danger' : 'success' ?> text-white text-uppercase fw-bold">
                        <?= htmlspecialchars($pet['status']) ?>
                    </div>
                    <div class="card-body">
                        <h4 class="card-title"><?= htmlspecialchars($pet['name']) ?></h4>
                        <h6 class="card-subtitle mb-2 text-muted">Type: <?= htmlspecialchars($pet['type']) ?></h6>
                        <p class="card-text"><?= nl2br(htmlspecialchars($pet['description'])) ?></p>
                        <small class="text-muted">Owner: <?= htmlspecialchars($pet['owner_name']) ?></small>
                    </div>
                    <?php if(isset($_SESSION['user_id']) && $_SESSION['user_id'] === $pet['owner_id']): ?>
                        <div class="card-footer bg-transparent border-top-0">
                            <a href="index.php?controller=pet&action=edit&id=<?= $pet['id'] ?>" class="btn btn-sm btn-outline-primary">Edit</a>
                            <a href="index.php?controller=pet&action=delete&id=<?= $pet['id'] ?>" class="btn btn-sm btn-outline-danger float-end" onclick="return confirm('Delete this listing?');">Delete</a>
                        </div>
                    <?php endif; ?>
                </div>
            </div>
        <?php endforeach; ?>
    <?php endif; ?>
</div>

<?php if (isset($totalPages) && $totalPages > 1): ?>
    <nav aria-label="Pet pagination" class="mt-4">
        <ul class="pagination justify-content-center">
            <li class="page-item <?= ($page <= 1) ? 'disabled' : '' ?>">
                <a class="page-link" href="index.php?<?= $queryString ?>&page=<?= $page - 1 ?>">Previous</a>
            </li>
            <?php for ($i = 1; $i <= $totalPages; $i++): ?>
                <li class="page-item <?= ($page == $i) ? 'active' : '' ?>">
                    <a class="page-link" href="index.php?<?= $queryString ?>&page=<?= $i ?>"><?= $i ?></a>
                </li>
            <?php endfor; ?>
            <li class="page-item <?= ($page >= $totalPages) ? 'disabled' : '' ?>">
                <a class="page-link" href="index.php?<?= $queryString ?>&page=<?= $page + 1 ?>">Next</a>
            </li>
        </ul>
    </nav>
<?php endif; ?>

<?php require_once 'views/templates/footer.phtml'; ?>
